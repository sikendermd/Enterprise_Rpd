DECLARE CONNECTION POOL "Oracle Analytics Usage"."Usage Tracking Connection Pool" AS "Usage Tracking Connection Pool"
	DATA SOURCE  {VALUEOF(OLAP_DSN)} 
	TIME OUT 300
	MAX CONNECTIONS 5
	TYPE 'Default' 
	USER 'VALUEOF(OLAP_USER)' 
	AES_PASSWORD '${deployed.OLAP_PASSWORD}'
	SHARED LOGIN
	CONNECTIONS TO SAME URI 10
	OUTPUT TYPE XML
	ISOLATION LEVEL DIRTY READ
	BULK INSERT BUFFER SIZE 32768 TRANSACTION BOUNDARY 10
	TEMP TABLE PREFIX {TT}  OWNER {} 
	DESCRIPTION {User name is VALUEOF(OLAP_USER)
Data source name: VALUEOF(OLAP_DSN)} 
	PRIVILEGES ( READ);
DECLARE CONNECTION POOL "Oracle Analytics Usage"."Usage Tracking Writer Connection Pool" AS "Usage Tracking Writer Connection Pool"
	DATA SOURCE  {VALUEOF(OLAP_DSN)} 
	TIME OUT 300
	MAX CONNECTIONS 5
	TYPE 'Default' 
	USER 'VALUEOF(OLAP_USER)' 
	AES_PASSWORD '${deployed.OLAP_PASSWORD}'
	SHARED LOGIN
	CONNECTIONS TO SAME URI 10
	OUTPUT TYPE XML
	ISOLATION LEVEL DIRTY READ
	BULK INSERT BUFFER SIZE 32768 TRANSACTION BOUNDARY 10
	TEMP TABLE PREFIX {TT}  OWNER {} 
	DESCRIPTION {User name is VALUEOF(OLAP_USER)
Data source name: VALUEOF(OLAP_DSN)} 
	PRIVILEGES ( READ);
DECLARE CONNECTION POOL "Oracle Data Warehouse"."Oracle Data Warehouse Connection Pool" AS "Oracle Data Warehouse Connection Pool" 
	DATA SOURCE  {valueof(OLAP_DSN)} 
	TIME OUT 300
	MAX CONNECTIONS 100
	TYPE 'OCI10G' 
	USER 'valueof(OLAP_USER)' 
	AES_PASSWORD '${deployed.OLAP_PASSWORD}'
	SHARED LOGIN
	CONNECTIONS TO SAME URI 10
	OUTPUT TYPE XML
	ISOLATION LEVEL DIRTY READ
	BULK INSERT BUFFER SIZE 32768 TRANSACTION BOUNDARY 10
	TEMP TABLE PREFIX {TT}  OWNER {} 
	DESCRIPTION {User name is VALUEOF(OLAP_USER)
Data source name is VALUEOF(OLAP_DSN)} 
	PRIVILEGES ( READ);
DECLARE CONNECTION POOL "Oracle Data Warehouse"."Oracle Data Warehouse Repository Initblocks Connection Pool" AS "Oracle Data Warehouse Repository Initblocks Connection Pool"
	DATA SOURCE  {valueof(OLAP_DSN)} 
	TIME OUT 300
	MAX CONNECTIONS 10
	TYPE 'Default' 
	USER 'valueof(OLAP_USER)' 
	AES_PASSWORD '${deployed.OLAP_PASSWORD}'
	SHARED LOGIN
	CONNECTIONS TO SAME URI 10
	OUTPUT TYPE XML
	BULK INSERT BUFFER SIZE 32768 TRANSACTION BOUNDARY 10
	TEMP TABLE PREFIX {TT}  OWNER {} 
	DESCRIPTION {User name is VALUEOF(OLAP_USER)
Data source name is VALUEOF(OLAP_DSN)} 
	PRIVILEGES ( READ);
DECLARE CONNECTION POOL "Oracle Data Warehouse"."CP_Proxy" AS "CP_Proxy"
	DATA SOURCE  {valueof(OLAP_DSN)} 
	TIME OUT 300
	MAX CONNECTIONS 100
	TYPE 'OCI10G' 
	USER 'valueof(OLAP_USER)' 
	AES_PASSWORD '${deployed.OLAP_PASSWORD}'
	SHARED LOGIN
	CONNECTIONS TO SAME URI 10
	OUTPUT TYPE XML
	ISOLATION LEVEL DIRTY READ
	BULK INSERT BUFFER SIZE 32768 TRANSACTION BOUNDARY 10
	TEMP TABLE PREFIX {TT}  OWNER {} 
	DESCRIPTION {User name is VALUEOF(OLAP_USER)
Data source name is VALUEOF(OLAP_DSN)} 
	PRIVILEGES ( READ);
DECLARE CONNECTION POOL "psd2sbl"."Connection Pool" AS "Connection Pool" 
	DATA SOURCE  {VALUEOF(OLTP_DSN)} 
	TIME OUT 300
	MAX CONNECTIONS 10
	TYPE 'Default' 
	USER 'dhsie_kies_interface' 
	AES_PASSWORD '${deployed.OLTP_PASSWORD_DHSIE}'
	SHARED LOGIN
	CONNECTIONS TO SAME URI 10
	OUTPUT TYPE XML
	BULK INSERT BUFFER SIZE 131072 TRANSACTION BOUNDARY 1000
	TEMP TABLE PREFIX {TT}  OWNER {} 
	PRIVILEGES ( READ);
DECLARE CONNECTION POOL "Real Time OLTP"."Real Time OLTP Connection Pool" AS "Real Time OLTP Connection Pool"
	DATA SOURCE  {VALUEOF(OLTP_DSN)} 
	TIME OUT 300
	MAX CONNECTIONS 32
	TYPE 'Default' 
	USER 'VALUEOF(OLTP_USER)' 
	AES_PASSWORD '${deployed.OLTP_PASSWORD}'
	SHARED LOGIN
	CONNECTIONS TO SAME URI 10
	OUTPUT TYPE XML
	ISOLATION LEVEL DIRTY READ
	BULK INSERT BUFFER SIZE 32768 TRANSACTION BOUNDARY 10
	TEMP TABLE PREFIX {TT}  OWNER {} 
	DESCRIPTION {User name is VALUEOF(OLTP_USER)
Data source name: VALUEOF(OLTP_DSN)} 
	PRIVILEGES ( READ);
DECLARE CONNECTION POOL "Real Time OLTP"."Real Time OLTP init Connection Pool" AS "Real Time OLTP init Connection Pool"
	DATA SOURCE  {VALUEOF(OLTP_DSN)} 
	TIME OUT 300
	MAX CONNECTIONS 10
	TYPE 'Default' 
	USER 'SIEBEL' 
	AES_PASSWORD '${deployed.OLTP_PASSWORD}'
	SHARED LOGIN
	CONNECTIONS TO SAME URI 10
	OUTPUT TYPE XML
	BULK INSERT BUFFER SIZE 131072 TRANSACTION BOUNDARY 1000
	TEMP TABLE PREFIX {TT}  OWNER {} 
	PRIVILEGES ( READ);
DECLARE CONNECTION POOL "Real Time OLTP Interface"."Real Time OLTP Interface Connection Pool" AS "Real Time OLTP Interface Connection Pool" 
	DATA SOURCE  {VALUEOF(OLTP_DSN)} 
	TIME OUT 300
	MAX CONNECTIONS 32
	TYPE 'OCI10G' 
	USER 'dhsie_kies_interface' 
	AES_PASSWORD '${deployed.OLTP_PASSWORD_DHSIE}'
	SHARED LOGIN
	CONNECTIONS TO SAME URI 10
	OUTPUT TYPE XML
	ISOLATION LEVEL DIRTY READ
	BULK INSERT BUFFER SIZE 32768 TRANSACTION BOUNDARY 10
	TEMP TABLE PREFIX {TT}  OWNER {} 
	DESCRIPTION {User name is: dhsie_kies_interface
Data source name: VALUEOF(OLTP_DSN)} 
	PRIVILEGES ( READ);
DECLARE RP VARIABLE "OLTP_USER" AS "OLTP_USER" EXPRESSION {'${deployed.OLTP_USER}'} 
	DESCRIPTION {Database user used to connect to the transaction database. } 
	PRIVILEGES ( READ);
DECLARE RP VARIABLE "OLAP_USER" AS "OLAP_USER" EXPRESSION {'${deployed.OLAP_USER}'} 
	DESCRIPTION {Database user used to connect to the data warehouse} 
	PRIVILEGES ( READ);
DECLARE RP VARIABLE "OLAP_DSN" AS "OLAP_DSN" EXPRESSION {'${deployed.OLAP_DSN}'} 
	DESCRIPTION {Data Source Name of the Data Warehouse.   It is used in the Connection pool definitions, to normalize this information in the repository.} 
	PRIVILEGES ( READ);
DECLARE RP VARIABLE "OLTP_DSN" AS "OLTP_DSN" EXPRESSION {'${deployed.OLTP_DSN}'} 
	DESCRIPTION {Data Source Name of the Transaction Database.} 
	PRIVILEGES ( READ);
DECLARE RP VARIABLE "TBO" AS "TBO" EXPRESSION {'${deployed.TBO}'} 
	DESCRIPTION {Stores the table owner of the transaction database.} 
	PRIVILEGES ( READ);
DECLARE RP VARIABLE "OLAPTBO" AS "OLAPTBO" EXPRESSION {'${deployed.OLAPTBO}'} 
	DESCRIPTION {Data Warehouse table owner} 
	PRIVILEGES ( READ);
